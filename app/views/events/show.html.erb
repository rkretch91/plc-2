<h1>Events#show</h1>
<p>Find me in app/views/events/show.html.erb</p>
    <%= cl_image_tag @event.photos, height: 300, width: 400, crop: :fill %>
    <%= simple_form_for @event, url: booking_event_path(@event), method: :get do |i| %>
        <%= i.button :submit, class: "btn btn-primary" %>
    <% end %>
